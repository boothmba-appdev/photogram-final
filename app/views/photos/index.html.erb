<!DOCTYPE html>
<html>
  <head>
    <title>Photogram 小蓝书</title>
    
    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    
    <link rel="stylesheet" media="all" href="/assets/application-e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855.css" />
  </head>

  <body>
    <h1>List of Photos 照片列表</h1>

<% if @current_user %>
  <form action="/insert_photo" method="post" class="form-horizontal" enctype="multipart/form-data">
    <label for="browser_input">Image 照片</label>
    <input id="browser_input" type="file" name="query_image" placeholder="Upload a photo 上传照片...">

    <label for="browser_caption">Caption 标题</label>
    <textarea id="browser_caption" name="query_caption" placeholder="Enter a caption 输入标题..."></textarea>

    <button>Add photo 添加照片</button>
  </form>
<% end %>

<hr>

<table border="2">
  <tr>
    <th>Image 照片</th>
    <th>Caption 标题</th>
    <th>Owner 发布者</th>
    <th>Posted 发布时间</th>
    <th>Likes 点赞数</th>
    <th></th>
  </tr>

<% @list_of_photos.each do |a_photo| %>
    <tr>
      <td>
        <img src="<%=a_photo.image%>" class="img-responsive" width="500" height="380">
      </td>
    
      <td>
        <%=a_photo.caption%>
      </td>
    
      <td>
        <%=a_photo.owner.username%>
      </td>
    
      <td>
        <%= time_ago_in_words(a_photo.created_at) %> ago
      </td>
    
      <td>
        <%= a_photo.likes_count %>
      </td>
    
      <td>
        <a href="/photos/<%= %>">
          Show details 查看更多
        </a>
      </td>
    </tr>
    <% end %>
</table>
</body>
</html>
